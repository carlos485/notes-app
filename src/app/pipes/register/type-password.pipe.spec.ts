import { TypePasswordPipe } from './type-password.pipe';

describe('TypePasswordPipe', () => {
  it('create an instance', () => {
    const pipe = new TypePasswordPipe();
    expect(pipe).toBeTruthy();
  });
});
