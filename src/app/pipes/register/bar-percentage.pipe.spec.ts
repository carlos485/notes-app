import { BarPercentagePipe } from './bar-percentage.pipe';

describe('BarPercentagePipe', () => {
  it('create an instance', () => {
    const pipe = new BarPercentagePipe();
    expect(pipe).toBeTruthy();
  });
});
